import _Node
import _processing

def mainProcess(tower_data):
    
    #instance data
    choices = _Node.MasterNode("choices")
    choices.data[i] = _Node.Node()
    choices.data[i].next = tower.path[i+1].head